# Keep the balena push context lean and reproducible. Only the Rust source and
# the static portal page (ui-static/) are needed; VCS/build artifacts and the
# retired React UI tree must never enter the context.
.git
.github
.versionbot
.opencode
target
**/target
ui
**/*.rs.bk
docs
CHANGELOG.md
README.md
LICENSE
